# 의존성
node_modules

# 빌드 산출물
.next
out
build
*.tsbuildinfo
next-env.d.ts

# 환경 변수 (시크릿 포함)
.env
.env.local
.env.*.local

# 로그
logs/*
!logs/.gitkeep
*.log
npm-debug.log*

# EMMA (인포뱅크 온프레미스 에이전트) — 용량이 크고 원격 저장소 대상 아님
EMMA3/
EMMA_installer.exe

# 기관 관리자 자격증명 (평문 비밀번호 포함)
prisma/org-admin-*.txt

# 인증서 · 개인키 (보안상 GitHub에 올리지 않음)
cert/nanum_pri.der
cert/*.der
cert/*.key
cert/*.pem
cert/*.p12
cert/*.pfx

# OS / 에디터
.DS_Store
Thumbs.db
.vscode/
.idea/

# EMMA 설치본 · DB 덤프 · 개인정보 포함 파일 (git 제외)
EMMA V.3 for Java/
EMMA_unix_3_7_1/
TalkFile_nanumplus_dump.sql
emma-mo-keys-*.csv

# Claude 작업 중 원본 백업 (커밋 제외)
.nanumplus-backup/

# 커밋 메시지 임시파일
.commitmsg

# 기관 계정 목록(개인정보 가능) - 원격 제외
나눔플러스_기관계정목록.xlsx
